# Archive

This folder is for stale references that may still be useful when comparing
against older vendor or integration work. Files here are not the current source
of truth for firmware behavior.

- Use `../../README.md`, `../project-brief.md`, and `../architecture/` for
  current project documentation.
- `generated-pdf/` contains PDF exports kept only for historical reference.
- Do not restore archived interfaces into active code without checking the
  current runtime path and Keil project references first.
